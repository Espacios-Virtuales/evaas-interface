version: "3.9"
services:
  evaas-ui:
    build:
      context: .
      dockerfile: Dockerfile
    image: evaas-ui:prod
    container_name: evaas-ui
    ports:
      - "8181:80"
    restart: unless-stopped
